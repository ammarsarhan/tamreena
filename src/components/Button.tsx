const base = "text-xs px-4 py-2 rounded-md transition-all ";

const primary = base + "bg-black text-white hover:bg-gray-800";
const secondary = base + "";
const color = base + "";
const outline = base + "border-[1px] border-gray-300 hover:bg-gray-50";
const disabled = base + "bg-gray-200"

export const buttonStyle = {
    primary: primary,
    secondary: secondary,
    color: color,
    outline: outline,
    disabled: disabled
}
